doctype html
html(lang="en")
    head
        meta(charset="UTF-8")
        meta(http-equiv="X-UA-Compatible", content="IE=edge")
        meta(name="viewport", content="width=device-width, initial-scale=1.0")
        title Postar 
        link(rel="stylesheet", href="stylePost.css")
    body 

    form(action="/postagem", method= "post")  

        h1 Postagem 

        input(type="text", name="autor", maxlength="100", placeholder="Autor", required)
        input(type="text", name="descricao", maxlength="100", placeholder="Descricao", required)
        input(type="url", name="imagem", maxlength="100", placeholder="http://", required)
        //input(type="file", accept=".mp4", name="imagem", maxlength="100", placeholder="arquivo", required)

        div(class="botao")
            input(type="submit", value="postar")
            a(href="/")  inicio 
            